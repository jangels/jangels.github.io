source "https://rubygems.org"

# Use this for local preview only:
#   bundle install
#   bundle exec jekyll serve
# (GitHub Pages ignores the Gemfile and uses its own Jekyll toolchain.)
gem "jekyll", "~> 4.3"
gem "kramdown"
