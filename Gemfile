source "https://rubygems.org"

# Plain Jekyll (matches GitHub Pages' Jekyll 3.x toolchain).
# This site ships its own _layouts and uses no plugins/themes, so the
# full github-pages gem is not required.
gem "jekyll", "~> 3.9"
